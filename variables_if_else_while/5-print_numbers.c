#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
	int single;

	for (single = 0; single <= 9; single++)
	{
		putchar(single);
	}
	putchar('\n');
	return (0);
}
